[pytest]
minversion = 8.0
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Markers
markers =
    e2e: End-to-end tests requiring Playwright and running services
    integration: Integration tests requiring ClickHouse database
    unit: Unit tests (fast, isolated, no external dependencies)
    benchmark: Performance benchmark tests (deferred)

# pytest-asyncio configuration
# strict mode: only @pytest.mark.asyncio tests handled by pytest-asyncio
# E2E tests without marker are handled by pytest-playwright's event loop
asyncio_mode = strict

# Options
addopts =
    -ra
    -q
    --strict-markers
    --strict-config
    --tb=short

# Timeout
timeout = 300
timeout_method = thread

# Logging
log_cli = false
log_cli_level = INFO

# Coverage (when running with --cov)
[coverage:run]
branch = true
source = src
omit =
    tests/*
    scripts/*

[coverage:report]
show_missing = true
skip_covered = false
precision = 2
exclude_lines =
    pragma: no cover
    def __repr__
    raise AssertionError
    raise NotImplementedError
    if __name__ == .__main__.:
    if TYPE_CHECKING:
    @abstractmethod
